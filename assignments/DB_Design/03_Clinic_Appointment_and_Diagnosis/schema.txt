title Clinic Appointment and Diagnostics Program
doctors [icon: doctor, color: green] {
  id serial pk
  doctor_name VARCHAR(100)
  department VARCHAR(50)
  department_id INT
  specialized_category VARCHAR(100)
  experience_in_years INT
  qualification_details VARCHAR(100)
  createdAt timestamp
  updatedAt timestamp
}

patients [icon: user, color: blue] {
  id serial pk
  patient_name VARCHAR(100)
  email VARCHAR(100)
  password VARCHAR(50)
  age INT
  phone_number VARCHAR(20)
  address TEXT
  createdAt timestamp
  updatedAt timestamp
}

appointments [icon: phone, color: yellow] {
  id serial pk
  patient_id INT fk
  doctor_id INT fk
  status VARCHAR(50)
  type VARCHAR(50)
  appointment_datetime (timestamp)
}

consultations [icon: consul, color: orange] {
  id serial pk
  patient_id INT fk
  doctor_id INT fk
  appointment_id INT fk
  prescriptions TEXT
}

tests [icon: book, color: red] {
  id serial pk
  name VARCHAR(100)
  description TEST
  price INT
  payment_id INT fk
  type VARCHAR(50)
}

consultation_tests [icon: water, color: red] {
  id serial pk
  test_id INT fk
  consultation_id INT fk
  status VARCHAR(50)
  instructions VARCHAR(100)
  date date
  time timestamp
}

reports [icon: report, color: purple] {
  id serial pk
  patient_id INT fk
  doctor_id INT fk
  test_id INT fk
  type VARCHAR(100)
  details TEXT
  results TEXT
  date date
  time timestamp
}

payment [icon: money, color: black] {
  id serial pk
  patient_id INT fk
  appointment_id INT fk
  consultation_id INT fk
  payment_status VARCHAR(50)
  payment_mode VARCHAR(50)
  date date
  time timestamp
}


patients.id - payment.patient_id: [color: black]

patients.id - appointments.patient_id: [color: yellow]
doctors.id < appointments.doctor_id: [color: yellow]

payment.consultation_id - consultations.id: [color: black]
payment.appointment_id - appointments.id: [color: black]

consultations.appointment_id - appointments.id: [color: orange]
consultations.doctor_id > doctors.id: [color: orange]
consultations.patient_id > patients.id: [color: orange]

tests.payment_id - payment.id: [color: red]

reports.patient_id > patients.id: [color: purple]
reports.doctor_id > doctors.id: [color: purple]
reports.test_id > tests.id: [color: purple]

consultation_tests.test_id - tests.id: [color: green]
consultation_tests.consultation_id - consultations.id: [color: green]
